# Caminhos ignorados pelo secret scanning do GitHub.
# Apenas fixtures e exemplos — nunca ignore código de produção.
paths-ignore:
  - ".env.example"
  - "testes/auxiliares/**"
  - "observabilidade/**"
